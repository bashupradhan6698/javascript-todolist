/* WAP to find difference of two numbers */

function subtraction(num1 = 0, num2 = 0) { //parameters num1 and num2 num1=0 and num2=0 0 and 0 are default values

  console.log(`${num1} - ${num2} = ${num1 - num2}`);
}

subtraction(14, 4); //agruments 14 and 4
subtraction();